import { BasicModal } from './BasicModal'

export const DeleteModal = () => {
  return (
    <BasicModal>
      <div>
        <h1>Delete modal</h1>
        <button>Delete</button>
      </div>
    </BasicModal>
  )
}
